<!DOCTYPE html>
<html>
<head>
  <title>Chatbot Gemini</title>
  <style>
    body { font-family: sans-serif; padding: 30px; max-width: 600px; margin: auto; }
    textarea, button { width: 100%; padding: 10px; margin-top: 10px; }
    .chat-box { background: #f0f0f0; padding: 15px; border-radius: 8px; margin-top: 20px; }
  </style>
</head>
<body>
  <h1>Chatbot Gemini</h1>
  <form method="POST" action="/chat">
    <textarea name="prompt" rows="4" placeholder="Tulis pertanyaan..." required><%= prompt || '' %></textarea>
    <button type="submit">Kirim</button>
  </form>

  <% if (response) { %>
    <div class="chat-box">
      <h3>Jawaban:</h3>
      <p><%= response %></p>
    </div>
  <% } %>
</body>
</html>
